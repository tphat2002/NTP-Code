#include<iostream>

using namespace std;
int main()
{
    int so;
    cout << "Nhap so:  ";
    cin >> so;

    if (so % 2 == 0) cout << "So chan";
    else cout << "So le";
    return 0;
}